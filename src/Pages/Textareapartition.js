/*import React, { useState } from 'react';

function Textareapartition() {
  const [section1, setSection1] = useState('');
  const [section2, setSection2] = useState('');
  const [section3, setSection3] = useState('');

  return (
    <div>
      <input
        type="text"
        value={section1}
        onChange={(e) => setSection1(e.target.value)}
        maxLength={2} // Maximum length for the first section
        rows={3}
      />
      
      <input
        type="text"
        value={section2}
        onChange={(e) => setSection2(e.target.value)}
        maxLength={4} // Maximum length for the second section
      />
      
      <input
        type="text"
        value={section3}
        onChange={(e) => setSection3(e.target.value)}
        maxLength={3} // Maximum length for the third section
      />
    </div>
  );
}

export default Textareapartition;*/
